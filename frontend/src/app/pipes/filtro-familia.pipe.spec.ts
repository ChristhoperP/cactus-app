import { FiltroFamiliaPipe } from './filtro-familia.pipe';

describe('FiltroFamiliaPipe', () => {
  it('create an instance', () => {
    const pipe = new FiltroFamiliaPipe();
    expect(pipe).toBeTruthy();
  });
});
