import { FiltroPromoCategoriaPipe } from './filtro-promo-categoria.pipe';

describe('FiltroPromoCategoriaPipe', () => {
  it('create an instance', () => {
    const pipe = new FiltroPromoCategoriaPipe();
    expect(pipe).toBeTruthy();
  });
});
